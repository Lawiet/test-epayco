// este archivo es para configurar las variables de entorno

// const isProduction = process.env.ENV_TYPE == 'prod' || process.env.ENV_TYPE == 'production';
// const apiUri = process.env.API_URL || "http://127.0.0.1:3333/";
const apiUri = "http://127.0.0.1:3333";

export default {
  //isProduction,
  apiUri,
};



